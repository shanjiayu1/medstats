% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-survival.R
\name{plot_km}
\alias{plot_km}
\title{Plot Kaplan-Meier survival curve}
\usage{
plot_km(
  data,
  group_var = "1",
  time_var = "time_to_mse",
  status_var = "mse_status",
  legend_labs = NULL,
  legend_title = "组别",
  xlab = "随访时间 (月)",
  ylab = "累积达成 MSE 的患者比例 (\%)",
  palette = c("#B5D1E8", "#EB938F", "#A3D9A5"),
  xlim = c(0, 12),
  break_time = 3,
  pval_coord = c(8, 0.2),
  show_risk_table = FALSE,
  save_filename = "KM累积发生率曲线.png",
  plot_width = 8,
  plot_height = 6,
  plot_dpi = 600
)
}
\arguments{
\item{data}{A data frame containing survival data.}

\item{group_var}{Character string or NULL or \code{"1"}. Name of the grouping variable.
Use \code{NULL} or \code{"1"} for single-group analysis. Default is \code{"1"}.}

\item{time_var}{Character string. Name of the survival time variable.
Default is \code{"time_to_mse"}.}

\item{status_var}{Character string. Name of the event status variable (1 = event).
Default is \code{"mse_status"}.}

\item{legend_labs}{Character vector or NULL. Custom labels for legend. Default is \code{NULL}.}

\item{legend_title}{Character string. Legend title. Default is \code{"组别"}.}

\item{xlab}{Character string. X-axis label. Default is \code{"随访时间 (月)"}.}

\item{ylab}{Character string. Y-axis label. Default is \code{"累积达成 MSE 的患者比例 (\%)"}.}

\item{palette}{Character vector. Colors for groups. Default is \code{c("#B5D1E8", "#EB938F", "#A3D9A5")}.}

\item{xlim}{Numeric vector. X-axis limits. Default is \code{c(0, 12)}.}

\item{break_time}{Numeric. Time interval for x-axis breaks. Default is \code{3}.}

\item{pval_coord}{Numeric vector. Coordinates for p-value annotation. Default is \code{c(8, 0.2)}.}

\item{show_risk_table}{Logical. Whether to show risk table below the plot. Default is \code{FALSE}.}

\item{save_filename}{Character string. Output filename for the saved plot.
Default is \code{"KM累积发生率曲线.png"}.}

\item{plot_width}{Numeric. Plot width in inches. Default is \code{8}.}

\item{plot_height}{Numeric. Plot height in inches. Default is \code{6}.}

\item{plot_dpi}{Numeric. Plot resolution (DPI). Default is \code{600}.}
}
\value{
Invisibly returns \code{NULL}. The plot is displayed and saved to \code{save_filename}.
}
\description{
Generates a Kaplan-Meier cumulative incidence curve with optional risk table,
supporting both single-group and multi-group analyses.
}
\examples{
\dontrun{
library(survival)
lung$status2 <- ifelse(lung$status == 2, 1, 0)
plot_km(lung, group_var = "sex", time_var = "time", status_var = "status2")
}

}
