% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-process.R
\name{long_to_surv_data}
\alias{long_to_surv_data}
\title{Convert longitudinal data to survival analysis format}
\usage{
long_to_surv_data(
  data,
  id_var = "患者病案号",
  event_flag_var = "是否MSE",
  time_var = "re随访时间",
  baseline_vars = c("性别", "胸腺切除")
)
}
\arguments{
\item{data}{A data frame in long format with repeated measurements per subject.}

\item{id_var}{Character string. Name of the column identifying individual subjects.
Default is \code{"患者病案号"}.}

\item{event_flag_var}{Character string. Name of the column indicating event occurrence
(1 = event, 0 = no event). Default is \code{"是否MSE"}.}

\item{time_var}{Character string. Name of the column containing follow-up time values.
Default is \code{"re随访时间"}.}

\item{baseline_vars}{Character vector. Names of baseline variables to retain (takes the
first value per subject). Default is \code{c("性别", "胸腺切除")}.}
}
\value{
A data frame with one row per subject, containing:
\itemize{
\item All \code{baseline_vars} columns (first value per subject)
\item \code{status}: 1 if event occurred, 0 otherwise
\item \code{time}: time to first event (if status=1) or last follow-up (if status=0)
}
}
\description{
Transforms long-format longitudinal data into a wide survival dataset suitable for
Cox regression. For each subject, it determines whether an event occurred (status = 1)
and calculates the time to first event or censoring.
}
\details{
The endpoint event must be coded as numeric 1. Events at time 0 are excluded.
}
\examples{
\dontrun{
surv_data <- long_to_surv_data(
  data = my_clinical_data,
  id_var = "PatientID",
  event_flag_var = "is_event",
  time_var = "follow_up_time",
  baseline_vars = c("Sex", "Age")
)
}

}
