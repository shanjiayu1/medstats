% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-process.R
\name{make_table1}
\alias{make_table1}
\title{Create a baseline characteristics table (Table 1)}
\usage{
make_table1(data, vars, specific_vars = NULL, group_var = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector. Names of variables to include in the table.}

\item{specific_vars}{Character vector or NULL. Names of continuous variables that are
non-normally distributed (reported as median + IQR). Default is \code{NULL}.}

\item{group_var}{Character string or NULL. Name of the grouping variable for comparisons.
If \code{NULL}, no group comparison or p-values are shown. Default is \code{NULL}.}
}
\value{
A \code{gtsummary} table object.
}
\description{
Generates a publication-ready baseline table using \code{gtsummary::tbl_summary()},
with automatic handling of non-normally distributed variables (Wilcoxon rank-sum test
with Z statistic) and optional group comparisons.
}
\details{
\itemize{
\item Normally distributed continuous variables: mean (SD)
\item Non-normally distributed continuous variables (specified via \code{specific_vars}): median (P25, P75)
\item Categorical variables: n (p\%)
\item When \code{group_var} is provided, p-values are computed using t-test for normal
continuous variables and Wilcoxon rank-sum Z-test for \code{specific_vars}.
}
}
\examples{
\dontrun{
library(gtsummary)
make_table1(
  data = trial,
  vars = c("age", "marker", "stage", "grade"),
  specific_vars = c("marker"),
  group_var = "trt"
)
}

}
