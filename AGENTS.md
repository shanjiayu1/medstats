# AGENTS.md

本文件适用于 `medstats` 仓库及其全部子目录。

## 项目概况

- 这是一个面向临床与流行病学研究的 R 包，最低支持 R 4.1.0。
- 包源码位于 `R/`，测试位于 `tests/testthat/`，长篇文档位于 `vignettes/`。
- `DESCRIPTION` 是依赖和包元数据的唯一权威来源。
- `NAMESPACE` 和 `man/*.Rd` 由 roxygen2 生成，不要手工编辑。

## 修改原则

- 只修改完成当前任务所必需的文件，保留用户已有但无关的改动。
- 延续现有 R 风格：使用 `<-`、base pipe `|>`、两个空格缩进，并优先使用 `包名::函数名`。
- 新增或修改导出函数时，同步维护 roxygen2 文档、示例和 `@export` 标记。
- 公共函数应尽早验证输入，并给出明确、可测试的错误信息；错误通常使用 `call. = FALSE`。
- 不要静默改变统计定义、缺失值处理、参考水平、事件编码或返回对象结构。
- 新增运行时依赖前，先更新 `DESCRIPTION`；仅测试或示例所需的依赖放入 `Suggests`。
- 不提交真实患者数据、身份信息、凭据、临时输出或本地环境文件。

## 测试与验证

- 行为变更应在 `tests/testthat/` 中添加或更新对应测试，包括正常路径、边界条件和错误输入。
- 测试应确定、可重复，不依赖网络、当前日期、交互式设备或本机绝对路径。
- 可选依赖相关测试使用 `testthat::skip_if_not_installed()`。
- 修改后优先执行最小相关测试，再执行完整测试和包检查。

常用命令（在仓库根目录运行）：

```sh
Rscript -e "testthat::test_local('.')"
Rscript -e "roxygen2::roxygenise('.')"
R CMD check --no-manual .
```

若改动 roxygen 注释，先运行 `roxygenise()`，并检查生成的 `NAMESPACE` 与 `man/` 变更是否符合预期。最终交付时说明运行了哪些检查，以及任何因缺少依赖而未运行的检查。

## 文档与示例

- README、函数文档和用户可见错误信息默认使用清晰、简洁的英文；必要时可保留与临床数据字段对应的中文名称。
- 新功能应提供最小可运行示例；示例数据优先使用 R 或已声明依赖包自带的公开数据集。
- 函数签名、默认值或返回结构变化时，同步更新 README 和相关 vignette。

## 统计与临床安全

- 统计方法的实现变化必须有测试支撑，并明确说明假设、事件定义、参考组及缺失值策略。
- 不把包输出描述为医学诊断或治疗建议。
- 处理示例或测试数据时使用合成数据或公开数据，禁止写入可识别个人身份的信息。
